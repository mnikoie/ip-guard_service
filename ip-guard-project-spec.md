# پروژه: IP Guard Service (سرویس نگهبان آی‌پی برای ویندوز)

## هدف کلی پروژه

یک نرم‌افزار حرفه‌ای برای ویندوز که **مثل یک سرویس واقعی ویندوز** رفتار کند (نه یک اسکریپت باز که با بستن ترمینال از کار بیفتد و نه یک برنامه‌ی معمولی که کاربر باید دستی اجرایش کند). این سرویس باید به‌طور دائم و در پس‌زمینه، از لحظه‌ی فعال‌سازی، آی‌پی عمومی سیستم را رصد کند و بر اساس منطق مشخصی، مجموعه‌ای از پروسس‌های نرم‌افزاری را فوراً و کامل (Force Kill) ببندد.

---

## نیازمندی‌های فنی و رفتاری (به‌ترتیب زمانی که مطرح شده‌اند)

### 1. رفتار سرویس ویندوز
- باید به‌صورت یک **Windows Service** واقعی نصب شود (قابل دیدن و مدیریت در `services.msc`).
- باید از لحظه‌ی فعال‌سازی، بدون وقفه در پس‌زمینه اجرا بماند، حتی بعد از ری‌استارت سیستم (Startup type = Automatic).
- باید امکان نصب/حذف/ری‌استارت آسان داشته باشد (ترجیحاً batch fileهای جدا برای هرکدام).

### 2. منطق اصلی تشخیص آی‌پی
- به‌صورت مداوم آی‌پی عمومی سیستم را چک کند.
- اگر آی‌پی سیستم به **رنج آی‌پی ایران** تغییر کند، باید بلافاصله وارد حالت واکنش شود.
- **قانون نهایی (fail-closed / امنیت‌محور):**
  - اگر کشور تشخیص‌داده‌شده در لیست کشورهای امن (trusted, مثل US, GB, ...) باشد -> هیچ کاری نکن.
  - در **هر حالت دیگر** -> کیل کن. این شامل موارد زیر است:
    - آی‌پی مستقیماً ایران (IR) تشخیص داده شود.
    - کشور ناشناخته یا خارج از لیست امن باشد.
    - سرویس تشخیص آی‌پی (API) خطا بدهد، تایم‌اوت شود، یا هیچ‌کدام از endpointها جواب ندهند.
  - خلاصه: **پیش‌فرض روی کیل است، مگر اینکه با اطمینان کامل ثابت شود IP در یک کشور امن قرار دارد.**

### 3. نحوه‌ی بستن برنامه‌ها (Kill)
- بستن باید **Force Kill کامل** باشد (نه بستن نرم/Close عادی پنجره)، یعنی معادل دستور:
  ```
  taskkill /IM "app.exe" /F /T
  ```
  (`/F` = خاتمه‌ی اجباری فوری، `/T` = بستن زیرپروسس‌ها هم).
- لیست برنامه‌های هدف باید در یک فایل تنظیمات (config) جدا و قابل ویرایش باشد، نه هاردکد در کد.
- **لاگ‌گیری هوشمند:** اگر برنامه‌ای در لیست هدف اصلاً در حال اجرا نبود (پس taskkill نتوانست چیزی ببندد)، **هیچ لاگ خطایی ثبت نشود**. فقط وقتی یک پروسس واقعاً و با موفقیت کشته شد، یک خط لاگ (`KILLED: xxx.exe`) نوشته شود.

### 4. سرعت واکنش (نیازمندی حیاتی)
- تشخیص تغییر آی‌پی باید تا حد امکان سریع باشد (محدودیت واقعی: وابسته به rate limit سرویس‌های IP lookup بیرونی، پس نمی‌تواند به چند میلی‌ثانیه برسد).
- **کیل کردن باید مداوم و پیوسته باشد، نه فقط یک‌بار در لحظه‌ی تشخیص.** یعنی اگر کاربر برنامه‌ی بسته‌شده را دوباره باز کند در حالی که آی‌پی هنوز ناامن است، باید در کسری از ثانیه (چند ده میلی‌ثانیه، مثلاً هر ۱۰۰ میلی‌ثانیه) دوباره و بلافاصله کشته شود - به‌طوری که کاربر عملاً نتواند برنامه را دوباره باز نگه دارد.
- این یعنی باید دو تایمر جدا وجود داشته باشد:
  1. تایمر چک آی‌پی (کندتر، چون به شبکه وابسته است).
  2. تایمر کیل مداوم (بسیار سریع، کاملاً محلی، فقط وقتی وضعیت "ناامن" است فعال می‌شود و تا برگشتن به وضعیت امن متوقف نمی‌شود).

### 5. مشکل واقعی که باید حل شود: قطع‌شدن تشخیص بعد از تغییر VPN
- وقتی کاربر یک VPN را قطع و دوباره وصل می‌کند، سرویس دیگر نمی‌توانست آی‌پی جدید را تشخیص دهد و فقط پیام "timeout" می‌داد.
- **علت:** استفاده‌ی پیش‌فرض از HTTP keep-alive در کتابخانه‌های HTTP request؛ وقتی رابط شبکه (VPN adapter) عوض می‌شود، Socket قدیمی به یک رابط مرده وصل می‌ماند و هر درخواست بعدی روی همان Socket تایم‌اوت می‌خورد، حتی وقتی اینترنت واقعاً برگشته است.
- **راه‌حل مورد استفاده:** غیرفعال‌کردن keep-alive و ساختن یک Agent کاملاً تازه برای هر درخواست (`keepAlive: false` + هدر `Connection: close`)، تا هر چک همیشه یک اتصال نو روی رابط شبکه‌ی فعلی باز کند.
- همچنین باید چند endpoint جایگزین (fallback) برای تشخیص IP/کشور وجود داشته باشد (نه فقط یک سرویس)، تا اگر یکی جواب نداد یا rate-limit شد، بعدی امتحان شود.

### 6. کاهش اسپم در لاگ خطا
- قبلاً هر بار که lookup آی‌پی fail می‌شد (مثلاً هر ۴-۵ ثانیه)، یک خط ارور جدید در لاگ نوشته می‌شد.
- **رفتار مطلوب:** فقط اولین خطای یک رشته‌ی پیوسته از fail لاگ شود؛ در ادامه سیستم در سکوت (بدون لاگ تکراری) به تلاش ادامه دهد تا موفق شود، و وقتی موفق شد یک خط لاگ "recovered" با تعداد تلاش‌های ناموفق ثبت شود.

### 7. هشدار بصری روی صفحه (Overlay Alert)
- کاربر می‌خواهد به‌محض تشخیص وضعیت ناامن (IR یا شناسایی‌نشده)، یک **نوار هشدار در بالای صفحه‌ی ویندوز** (شبیه Alarm) ظاهر شود تا خودش سریع بفهمد و بتواند نرم‌افزار را ببندد.
- ظاهر نوار باید حرفه‌ای/زیبا باشد (رنگ برجسته مثل قرمز، متن واضح، شکل مرتب).
- باید متن فارسی مشخص کند که آی‌پی تغییر کرده / شناسایی نشده، و IP و کد کشور فعلی را نشان دهد.
- **محدودیت فنی مهم که باید در نظر گرفته شود:** یک Windows Service به‌دلیل Session 0 Isolation نمی‌تواند مستقیماً روی دسکتاپ کاربر پنجره رسم کند. راه‌حل: یک برنامه‌ی جدا و سبک (overlay) که زیر حساب کاربری اجرا می‌شود و از طریق یک فایل وضعیت مشترک (که سرویس اصلی می‌نویسد) با سرویس هماهنگ می‌شود.
- به محض بازگشت آی‌پی به کشور امن، نوار هشدار باید فوراً مخفی/جمع شود.

---

## خلاصه معماری پیاده‌سازی‌شده (Node.js + Windows)

| بخش | فناوری | نقش |
|---|---|---|
| سرویس اصلی | Node.js + `node-windows` | چک آی‌پی، تشخیص کشور، اجرای taskkill، نوشتن status.json و لاگ |
| نصب/حذف سرویس | batch files + node-windows | نصب/حذف/ری‌استارت سرویس با یک کلیک |
| تشخیص IP | axios + چند endpoint (ip-api.com, ipapi.co) با Agent بدون keep-alive | مقاوم در برابر تغییر VPN و قطعی یک سرویس |
| کیل پروسس | `taskkill /F /T` در دو تایمر (چک IP کند + کیل سریع مداوم) | بستن کامل و مستمر برنامه‌ها |
| هشدار بصری | فایل HTA (HTML Application - بومی ویندوز، بدون نیاز به نصب) | نوار قرمز بالای صفحه که با پایش یک فایل status.json نمایش/مخفی می‌شود |
| پیکربندی | `config.json` | لیست برنامه‌های هدف، کشورهای امن، فاصله‌ی تایمرها، مسیر لاگ/وضعیت |

---

## نکات باز / تصمیم‌های آینده

- روش تشخیص «آی‌پی ایران» فعلاً از طریق کد کشور (`countryCode`) با فراخوانی سرویس‌های بیرونی (ip-api.com / ipapi.co) است. یک گزینه‌ی مطرح‌شده اما هنوز اجرا نشده: استفاده از یک دیتابیس رنج CIDR رسمی آی‌پی‌های ایران به‌صورت آفلاین (بدون وابستگی به اینترنت برای تشخیص جغرافیایی) یا ترکیب هر دو روش.
- منطق فعلی fail-closed یعنی اگر صرفاً اینترنت قطع شود (نه واقعاً IP تغییر کرده باشد)، سیستم به‌اشتباه وضعیت "ناامن" را فعال می‌کند و هم کیل و هم هشدار بصری اجرا می‌شود؛ این یک trade-off عمدی بین امنیت بیشتر و false positive بیشتر است.
